# Production copy of previous settings.py values
# This file is a direct copy of the hard-coded values that were present in settings.py before switching to env-based config.
# Review and replace any sensitive values before using in a real production environment.

# Core
DJANGO_SECRET_KEY=django-insecure-onecup-one-tree-secret-key-change-in-production
# In production DEBUG should be disabled
DJANGO_DEBUG=False
# Enable production to switch settings.py to Postgres configuration
DJANGO_PRODUCTION=True
# Comma-separated hosts for production
DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Postgres example values (replace these with real credentials)
POSTGRES_DB=onecup_production_db
POSTGRES_USER=onecup_prod_user
POSTGRES_PASSWORD=ReplaceThisWithAStrongPassword
POSTGRES_HOST=db.example.com
POSTGRES_PORT=5432

# Email configurations for setting emails
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=janon3030@gmail.com
EMAIL_HOST_PASSWORD="bflu atpq mrhe wzvw"
DEFAULT_FROM_EMAIL=janon3030@gmail.com
CONTACT_NOTIFICATION_EMAIL=djanonelhard@gmail.com

# Notes:
# - This file contains secrets (SECRET_KEY, EMAIL_HOST_PASSWORD). Do NOT commit to source control.
# - For a real production deployment, ensure `DJANGO_PRODUCTION=True` and fill the `POSTGRES_*` values above.
# - Prefer using server environment variables or a secrets manager instead of a file with plaintext secrets.
